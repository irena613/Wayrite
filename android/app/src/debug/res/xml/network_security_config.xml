<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only: allows plain HTTP (cleartext) to the Firebase Local Emulator
  Suite, which serves Firestore/Auth/Functions over HTTP rather than HTTPS.
  10.0.2.2 is the Android emulator's alias for the host machine's localhost.
  This file only applies to debug builds (android/app/src/debug/), so release
  builds keep Android's default cleartext-blocked policy.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
